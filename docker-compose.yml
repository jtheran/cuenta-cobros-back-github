services:
  base:
    container_name: base-nodejs
    build: .
    environment:
      - NODE_ENV=development
      - DATABASE_URL=mysql://root:root_pass_lsv@201.219.216.217:3324/db_lsv
    ports:
      - '3495:4000'
    volumes:
      - .:/base
      - /base/node_modules
      - ./.env:/base/.env
    command: >
      sh -c "npx prisma migrate deploy && npx prima generate && npm run start"